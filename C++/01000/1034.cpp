#include <iostream>
#include <vector>
using namespace std;

int main() {
    int N, M;
    cin >> N >> M;

    vector <string>vec;

    for(int i=0; i<N; i++) {
        string s;
        cin >> s;
        vec.push_back(s);
    }
}